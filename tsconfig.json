{
  "compilerOptions": {
    "sourceMap": true,
    // TSをES5に変換
    "target": "es5",
    // TSのモジュールをES Modulesとして変換する
    // これを指定しないと、TypeScriptコードのimport文とexport文がコンパイラによってCommonJSとして変換されるため
    // webpackによるTree Shaking（未使用のimportを静的解析によって振り落とす機能）のメリットが得られない
    "module": "es2015"
  }
}
